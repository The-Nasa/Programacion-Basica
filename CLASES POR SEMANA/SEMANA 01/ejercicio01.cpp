#include <iostream>

using namespace std;
int main()
{
    int x;
    if (x == 0)
        cout << "es cero ";
    else if (x > 0)
        cout << "es mayor que cero";
    else
        (x < 0)
                cout<< "es menor que cero";

    return 0;
}